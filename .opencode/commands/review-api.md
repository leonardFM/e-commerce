---
description: Review API implementation against layered architecture and endpoint requirements.
agent: code-reviewer
---

Review the API implementation for `$ARGUMENTS`.

Focus on route handlers under `app/api`, services and repositories under `modules`, validation schemas, status codes, and response consistency.

Return findings ordered by severity with file references.
